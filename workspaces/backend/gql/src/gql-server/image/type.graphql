union ImageResponse = Image | ImageError

type ImageError implements Error {
  message: String!
}

type Image {
  id: ID!
  name: String
  description: String
  createdAt: DateTime!
  file: File!
  asset: Asset!
}
