{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Dev Docker Compose up",
      "type": "shell",
      "command": "docker compose -f docker-compose.yml -f dev.docker-compose.yml up"
    },
    {
      "label": "Dev Docker Compose down",
      "type": "shell",
      "command": "docker compose -f docker-compose.yml -f dev.docker-compose.yml down"
    }
  ]
}
